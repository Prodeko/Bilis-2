type Player = {
    id: Uint16Array
    first_name: string
    last_name: string
    elo: Uint16Array
    fav_color: string
    nickname: string
    fav_ball: string
    emoji: string
}

export type {Player};
