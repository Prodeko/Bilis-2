{
  "name": "bilis-2",
  "version": "1.0.0",
  "repository": "git@github.com:Prodeko/Bilis-2.git",
  "private": true,
  "scripts": {
    "dev": "docker-compose -f docker-compose.dev.yml up --build --remove-orphans",
    "dev:migrate:up": "docker-compose -f docker-compose.dev.yml exec next yarn ts-node ./server/utils/migrator up",
    "dev:migrate:create": "docker-compose -f docker-compose.dev.yml exec next yarn ts-node ./server/utils/migrator create --folder ./server/migrations --name new-migration.ts",
    "dev:migrate:down": "docker-compose -f docker-compose.dev.yml exec next yarn ts-node ./server/utils/migrator down",
    "dev:migrate:pending": "docker-compose -f docker-compose.dev.yml exec next yarn ts-node ./server/utils/migrator pending",
    "dev:migrate:executed": "docker-compose -f docker-compose.dev.yml exec next yarn ts-node ./server/utils/migrator executed",
    "dev:create-test-data": "docker-compose -f docker-compose.dev.yml exec next yarn ts-node ./server/utils/createTestData",
    "dev:redis:test": "docker-compose -f docker-compose.dev.yml exec next yarn ts-node ./server/utils/testField",
    "test": "docker-compose -f docker-compose.test.yml up --remove-orphans --build",
    "build-dev": "docker-compose -f docker-compose.build-dev.yml up --build --remove-orphans"
  }
}
